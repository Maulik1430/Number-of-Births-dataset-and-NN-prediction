# Number-of-Births-dataset-and-NN-prediction
This repository demonstrates time-series feature engineering and neural network implementation on a birth dataset. Date features were extracted, rolling window statistics were created, and cyclic encoding was applied to capture seasonal and temporal patterns before training the model.
